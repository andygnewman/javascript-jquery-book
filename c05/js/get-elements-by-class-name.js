var elements = document.getElementsByClassName('hot'); // Find hot itemsif (elements.length > 2) {                            // If 3 or more are found// an example of looping through the nodelist to change all elements.  // for (i = 0; i < elements.length; i++) {  //   elements[i].className = 'cool';  // }  var el = elements[2];              // Select the third one from the NodeList  el.className = 'cool';             // Change the value of its class attribute}// trying this will result in 'cannot set property className of undefined'// as it is a live nodelist and due to rpevious change there is no elements[2]  // elements[2].className = 'normal';// however this will change it to normal as el is set to the reference for the//object that was is position [2] in the nodelist.  // el.className = 'normal';